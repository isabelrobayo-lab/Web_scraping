# Runbook: Migraciones de base de datos

## Estado actual

**No aplicable**. El proyecto no utiliza base de datos relacional.

## Futuro

Si se añade una base de datos en el futuro:

1. Usar un ORM o migrator (ej. `node-pg-migrate`, `Prisma migrate`)
2. Documentar el comando de ejecución aquí
3. Añadir paso de migración al runbook de despliegue (si aplica)

## Placeholder

```
# Cuando existan migraciones:
# npm run migrate
# o: npx node-pg-migrate up
```
